# Task 2 evidence index

Populate this index as evidence is generated. Do not transcribe metrics manually when a generating artefact can be linked instead.

| ID | Report section / KSB | Claim or decision evidenced | Source artefact | Generating command | Git commit | Status |
|---|---|---|---|---|---|---|
| PF-01 | 2a; K6, S25 | Scope, configuration and delivery controls were frozen before implementation | `config/experiment.yaml`, `docs/preflight.md` | Not applicable | Not committed | Ready for review |
| DP-01 | 2b; K4, S25, B3 | The public data-pipeline interface minimises, deduplicates, joins, splits and writes its evidence bundle on synthetic Amazon-style records | `tests/test_acquisition.py`, `src/amazon_sentiment/data_pipeline.py` | `.venv/bin/python -m pytest -q` | Not committed | 43 tests passing across the repository |
| DS-01 | 2b; K4, B3 | Dataset provenance, minimisation, observed source counts and chronological split | `artifacts/manifests/{data_manifest,split_summary}.json` | `.venv/bin/python -m amazon_sentiment prepare ...`; aggregate allowlist export | Not committed | Complete; public aggregate exports contain no review text or row-level identifiers |
| DQ-01 | 2b; K4, K24 | Data-quality, deduplication and join findings | `artifacts/manifests/{data_quality,join_report}.json` | `.venv/bin/python -m amazon_sentiment prepare ...`; aggregate allowlist export | Not committed | Complete; public aggregate exports contain no review text or row-level identifiers |
| BM-01 | 2b; S25 | Repeated equivalent-row benchmark selected Zstandard Parquet with 65,536-row groups | `artifacts/metrics/storage_benchmark.csv`, `storage_benchmark_environment.json`, `storage_benchmark_decision.json` | `.venv/bin/python -m amazon_sentiment benchmark ...` | Not committed | Ready for review |
| ML-01 | 2b; K1, K5, S26 | Majority, TF-IDF and DistilBERT comparison on the untouched test set | `artifacts/metrics/baseline_model_selection_metrics.json`, `final_test_metrics.json` | `.venv/bin/python -m amazon_sentiment baselines ...`; `evaluate-test` | Not committed | Complete; protected test result is immutable |
| TP-01 | 2b/2c; K1, K4, S25 | Pinned-tokenizer length coverage selected 256 without reading policy-calibration or test rows | `artifacts/metrics/distilbert_token_length_{summary.json,slices.csv}`, `config/distilbert.yaml` | `.venv/bin/python -m amazon_sentiment token-length ...` | Not committed | Complete; 256 selected at 95.17% coverage and the standalone throughput gate was waived explicitly |
| TP-02 | 2b; K4, S25 | The optional throughput gate was explicitly skipped; token coverage fixes 256 while batch 8/accumulation 4 remains an unmeasured conservative assumption | `artifacts/metrics/distilbert_training_decision.json` | Not applicable; decision record | Not committed | Exception recorded; do not claim measured GPU throughput |
| TR-01 | 2b; K1, S25, S26 | Governed DistilBERT fine-tuning excludes policy/test rows, resumes compatible epoch checkpoints and exports a reloadable private bundle plus validation logits | `src/amazon_sentiment/transformer.py`, `tests/test_transformer_training.py`; private Drive `distilbert-run/` | Colab notebook calls `python -m amazon_sentiment train-transformer ...` | Not committed | Complete; two epochs on T4 and hash-verified local handoff |
| EV-01 | 2b/2c; K5, K24 | Calibration, uncertainty and selective-routing trade-off | `artifacts/metrics/{policy_calibration_summary,final_test_metrics}.json`, `artifacts/figures/{model_comparison,calibration_comparison,routing_policy}.svg` | `.venv/bin/python -m amazon_sentiment evaluate-test`; `evaluation-evidence` | Not committed | Complete; neutral routing limitation recorded |
| EF-01 | 2b; K1, S25, S26 | Local CPU batch-one latency and serialised serving size inform the deployment decision | `artifacts/metrics/inference_benchmark.json`, `deployment_recommendation.json` | `.venv/bin/python -m amazon_sentiment evaluation-evidence` | Not committed | Complete; DistilBERT selected with TF-IDF fallback |
| SL-01 | 2c; K12, K24 | Robustness and performance-disparity findings across seven declared slice families | `artifacts/metrics/final_test_slice_metrics.csv`, `artifacts/figures/slice_macro_f1_ranges.svg` | `.venv/bin/python -m amazon_sentiment evaluate-test`; `evaluation-evidence` | Not committed | Aggregate slice evidence complete; do not infer demographic fairness |
| AC-01 | 2c; K29 | Scoped accessibility implementation review and browser audit | `artifacts/accessibility/accessibility-checklist.md` | Contrast calculation, offline implementation checks, live endpoint smoke test and manual browser audit (Windows, Edge 153, Narrator, Lighthouse) | Not committed | Complete; keyboard, accessibility-tree, screen-reader, Lighthouse, 200% zoom and 320px viewport checks passed on 24 September 2026 after three fixes; single environment only; no WCAG conformance claim |
| UI-01 | 2d; S5, K21 | Artefact operating with confident, review-required and error states | `src/amazon_sentiment/inference.py`, `interface.py`, `app.py`, `tests/test_inference.py` | `.venv/bin/python -m pytest -q tests/test_inference.py`; `python app.py` | Not committed | Complete; real DistilBERT and live Gradio endpoint smoke checks verify the required states; screenshot evidence was not produced |
| HO-01 | 2d; K21 | Model disclosure, installation, operation, recovery and accessibility-test guidance | `README.md`, `docs/model-card.md`, `docs/runbook.md` | Current-workspace tests and smoke checks | Not committed | Complete within scope; fresh-clone rehearsal excluded; browser accessibility checks performed and recorded in AC-01 |
