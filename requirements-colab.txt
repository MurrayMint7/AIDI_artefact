# Install this lock only into the notebook's project dependency directory.
# PYTHONPATH exposes these pins to training subprocesses while Colab's
# CUDA-enabled torch remains available and the notebook kernel stays untouched.
-r requirements-transformer.txt
