# Install this lock only inside the notebook's isolated virtual environment.
# The venv inherits Colab's CUDA-enabled torch but shadows other managed
# packages, so exact project pins cannot break google-colab, gcsfs or bigframes.
-r requirements-transformer.txt
